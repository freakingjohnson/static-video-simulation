create table cards (
    id serial primary key,
    card_name text,
    card_desc text,
    card_type text,
    item_type text,
    card_level text,
    bad_stuff text,
    how_many text,
    picture_url text
)